# Functions in Python
# Difference between Parameter and Argument
# Defining Input Type and Default Value

def greet(first_name, last_name): # : to specify type, = to specify default value
    print(f"Hi {first_name} {last_name}")

greet("Arihant", "Bhandari")